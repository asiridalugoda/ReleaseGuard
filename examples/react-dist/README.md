# Example: React dist/

This directory simulates a typical React production build output for use with `make demo`.

Run:
```bash
releaseguard check ./examples/react-dist
releaseguard fix ./examples/react-dist
```
